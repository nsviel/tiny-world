# Assets

Optional visual assets used by the Pygame renderer.

Supported files:

```text
trees/tree.png
food/berries.png
```

PNG files should use a transparent background. If a file is missing, TinyWorld uses its procedural drawing instead.
